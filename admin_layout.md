# ICONA Super Admin Dashboard

See [`docs/admin_layout.md`](docs/admin_layout.md) for the canonical spec -
role split (`SUPER_ADMIN` vs. a tenant's own `ADMIN`), what's built vs. still
placeholder data (the LLM usage and audit log numbers are currently mocked,
not real), the tier-limit inconsistency to reconcile, and the backlog for
maximizing admin control and visibility.

This file is kept as a pointer, not a duplicate, so the two copies can't
drift out of sync - the same reason `CLAUDE.md` points at `AGENTS.md`.
